kubernetes>=26.0.0
# Using a recent version of the kubernetes client.
# Pinning to a major version is a good practice.
boto3>=1.28.0 # For AWS SDK interaction, EKS describe_cluster, STS token generation
awscli>=1.29.0 # For reference, and potentially for `aws eks get-token` if direct boto3 proves too complex for token generation.
